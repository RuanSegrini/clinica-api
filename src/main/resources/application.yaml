spring:
  application:
    name: clinica-api

  datasource:
    url: jdbc:postgresql://localhost:5433/clinicadb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080